apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.manifest.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.manifest.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.manifest.name }}
    spec:
      containers:
        - name: {{ .Values.manifest.name }}
          image: {{ .Values.image }}
          ports:
            - containerPort: 7054
          args:
            - sh
            - -c
            - |
              fabric-ca-server start \
                -b {{ .Values.user }}:{{ .Values.pass }} \
                -u http://{{ .Values.root.user }}:{{ .Values.root.pass }}@{{ .Values.root.host }} \
                -d